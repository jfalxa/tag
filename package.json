{
  "name": "tag-query",
  "version": "1.1.1",
  "main": "./lib/index.js",
  "license": "MIT",
  "devDependencies": {
    "pfft-preset-cli": "^0.18.3"
  },
  "dependencies": {
    "lodash": "^4.17.4",
    "pegjs": "^0.10.0"
  },
  "ava": {
    "babel": "inherit",
    "require": [
      "pfft-partial-ava/config/setup"
    ]
  },
  "scripts": {
    "build": "cli build",
    "clean": "cli clean",
    "coverage": "cli coverage",
    "start": "cli start",
    "test": "cli test"
  }
}
